local _M = {
    base_url = require('utils.base_url').base_url,
    substitute = require('utils.substitute').substitute,
    markets = require('utils.markets'),
    policy = require('utils.policy'),
    supplements = require('utils.supplements'),
    misc = require('utils.misc'),
    debug = require('utils.debug')
}

return _M
